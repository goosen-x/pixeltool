# ================================
# PixelTool Environment Configuration
# ================================
# Copy this file to .env.local and fill in your values

# ================================
# SITE CONFIGURATION
# ================================
NEXT_PUBLIC_SITE_URL=https://pixeltool.pro
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ================================
# DATABASE CONFIGURATION
# ================================
# Postgres в Docker на проде (контейнер pixeltool-db, сеть pixeltool-net,
# без публикации порта на хост — доступен только другим контейнерам в сети).
# Для локальной разработки подними свой Postgres и укажи его сюда.
DATABASE_URL=postgresql://pixeltool:password@localhost:5432/pixeltool

# ================================
# SMTP (лид-магнит: отправка PDF на email из сайдбара, app/api/leads)
# ================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@pixeltool.pro

# ================================
# TELEGRAM (уведомления с форм: app/api/contact и app/api/feedback)
# ================================
# Один бот на оба маршрута. Без этих двух переменных обе формы отвечают
# ошибкой — сообщение никуда не уходит.
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
# HTTP-прокси до api.telegram.org, если хостинг блокирует прямой доступ
# (см. lib/telegram/proxy-fetch.ts). Формат: http://user:password@host:port
# Через запятую — несколько прокси с фолбэком. Пусто — обычный fetch напрямую.
TELEGRAM_PROXY_URL=

# ================================
# ANALYTICS
# ================================
NEXT_PUBLIC_YANDEX_METRIKA_ID=your-metrika-id

# ================================
# SEO & VERIFICATION (Optional)
# ================================
# NEXT_PUBLIC_GOOGLE_VERIFICATION=your-google-verification-code
# NEXT_PUBLIC_YANDEX_VERIFICATION=your-yandex-verification-code

# ================================
# PRODUCTION DEPLOYMENT
# ================================
# These are set automatically by deployment platforms:
# NODE_ENV=production
# VERCEL_URL (set by Vercel)
# PORT (for self-hosted deployments)

# Wordstat API (сбор частотности запросов, pnpm wordstat).
# Ключ сервисного аккаунта Yandex Cloud с ролью search-api.webSearch.user.
# Без этих переменных скрипт не запустится; на работу сайта они не влияют.
YANDEX_API_KEY=your-yandex-cloud-api-key
YANDEX_FOLDER_ID=your-yandex-cloud-folder-id
