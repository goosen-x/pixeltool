export function MorseCodeTranslatorSeo() {
	return (
		<div className='mx-auto mt-16 max-w-3xl space-y-12'>
			<section>
				<h2 className='text-2xl font-bold tracking-tight'>
					Почему при расшифровке нужен выбор языка
				</h2>
				<p className='mt-3 text-muted-foreground'>
					При переводе текста в код язык определяется сам: кириллица и
					латиница не пересекаются, поэтому «Hello Привет» кодируется
					целиком без ошибок, а выбирать ничего не нужно. С расшифровкой
					сложнее — азбука Морзе кодирует таблицу соответствий, а не буквы
					алфавита напрямую, и у русской и латинской таблиц одинаковые
					последовательности точек и тире означают разные буквы. Код «.--.»
					— это П в русской таблице и P в английской: символы разные, а
					сигнал один и тот же. Тул сам подбирает язык с меньшим числом
					нераспознанных символов, но переключатель ниже поля остаётся —
					поправить вручную, если сообщение короткое или намеренно смешанное.
				</p>
			</section>

			<section>
				<h2 className='text-2xl font-bold tracking-tight'>
					Как читать результат: пробелы и «/»
				</h2>
				<p className='mt-3 text-muted-foreground'>
					Один пробел между группами точек и тире разделяет буквы внутри слова,
					а символ «/» с пробелами по бокам разделяет слова. Например, «... ---
					... / ... --- ...» — это два слова «SOS SOS», а не одно длинное слово
					из шести букв.
				</p>
			</section>
		</div>
	)
}
