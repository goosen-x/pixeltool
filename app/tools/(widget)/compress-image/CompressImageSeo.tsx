export function CompressImageSeo() {
	return (
		<div className='mx-auto mt-16 max-w-3xl space-y-12'>
			<section>
				<h2 className='text-2xl font-bold tracking-tight'>
					Как устроено сжатие без сервера
				</h2>
				<p className='mt-3 text-muted-foreground'>
					Пересжатие делает браузер сам — через canvas, встроенный в каждый
					современный браузер механизм рисования и кодирования изображений.
					Файл рисуется на невидимом холсте и заново кодируется в JPEG или
					WebP с выбранным уровнем качества — точно так же, как это делают
					графические редакторы, только без установки программы и без
					загрузки фото на чужой сервер.
				</p>
			</section>

			<section>
				<h2 className='text-2xl font-bold tracking-tight'>
					Почему у PNG ползунок ничего не меняет
				</h2>
				<p className='mt-3 text-muted-foreground'>
					PNG — формат сжатия без потерь: он не умеет жертвовать деталями
					ради размера, поэтому параметра «качество» у него в принципе нет.
					Если исходник — PNG и нужен именно меньший файл, переключите
					формат на JPEG или WebP: они сжимают управляемо, теряя ровно
					столько деталей, сколько вы разрешите ползунком, а не всё или
					ничего.
				</p>
			</section>
		</div>
	)
}
