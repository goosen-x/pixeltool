/**
 * Реклама отключена: аккаунт заблокирован в РСЯ (15.07.2026).
 *
 * Флаг общий для блоков и для скриптов Яндекс.РТБ в корневом layout. Раньше
 * блоки скрывались сами, а context.js всё равно грузился на каждой странице:
 * PageSpeed показал 400 КБ рекламных бандлов (context.js плюс partner-code с
 * yastatic.net) там, где не рендерилось ни одного объявления.
 *
 * Чтобы вернуть рекламу, достаточно снять этот флаг.
 */
export const ADS_DISABLED = true
