/**
 * Реклама включена, но аккаунт РСЯ был заблокирован 15.07.2026 и на момент
 * возврата состояние неизвестно. Поэтому блоки устроены так, чтобы пустая
 * выдача не портила страницу: контейнер рендерится без зарезервированной
 * высоты и остаётся невидимым, пока Яндекс не вставит в него объявление.
 *
 * Когда подтвердится, что объявления приходят стабильно, имеет смысл вернуть
 * резервирование высоты (min-height под формат блока): это уберёт сдвиг
 * вёрстки в момент появления рекламы, но вернёт пустое место при отказе.
 */
export const ADS_DISABLED = false

/** Через сколько перестаём ждать объявление и оставляем блок свёрнутым. */
export const AD_RENDER_TIMEOUT_MS = 5000
