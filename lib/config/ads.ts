/**
 * Реклама включена. Аккаунт РСЯ был заблокирован 15.07.2026, но к 21.09.2026
 * объявления показываются стабильно — это подтвердил и живой рендер блока
 * (/tools/draw-lots), и Search Console: CLS 0,54 на всех 47 страницах тулов
 * на десктопе, потому что AdSection схлопывался в ноль до заполнения и резко
 * раздвигал вёрстку, когда реклама приходила. Высота под AdSection теперь
 * зарезервирована (см. components/ads/AdSection.tsx) — это убирает сдвиг, но
 * при отказе рекламы (адблок, пустая выдача) оставляет пустое место в
 * сайдбаре вместо схлопывания.
 */
export const ADS_DISABLED = false

/** Через сколько перестаём ждать объявление и оставляем блок свёрнутым. */
export const AD_RENDER_TIMEOUT_MS = 5000
